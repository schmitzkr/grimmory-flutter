name: grimmory
description: "Audiobook client for self-hosted Grimmory servers"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^3.4.2

  # Navigation
  go_router: ^18.0.0

  # HTTP
  dio: ^5.8.0+1

  # Token / server-URL storage
  flutter_secure_storage: ^10.3.1
  shared_preferences: ^2.5.2

  # OIDC login (Authentik or any IdP a self-hosted Grimmory instance is wired
  # to) — only oidc_core's low-level PKCE/request utilities are used (via
  # `oidc`, which re-exports oidc_core), not the full OidcUserManager, since
  # Grimmory's /auth/oidc/callback wants the raw PKCE result rather than a
  # pre-exchanged token. No oidc_default_store — that's only needed to back
  # OidcUserManager's own session/token store, unused here.
  oidc: ^0.13.0
  app_links: ^7.2.1
  url_launcher: ^6.3.0

  # Models
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

  # Background audio + Android Auto (MediaBrowserService) surface
  audio_service: ^0.18.15
  just_audio: ^0.10.6
  audio_session: ^0.2.4
  # Already a transitive dep of audio_service — declared directly since the
  # audio handler imports BehaviorSubject itself (for chapter-list state
  # alongside audio_service's own queue/mediaItem/playbackState subjects).
  rxdart: ^0.28.0

  # Misc
  package_info_plus: ^10.2.0
  cached_network_image: ^4.0.0
  cupertino_icons: ^1.0.8
  # Private, permission-free storage location for offline audiobook downloads
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3
  build_runner: ^2.16.0
  freezed: ^4.0.1
  json_serializable: ^6.9.3

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  image_path: "assets/icon/icon.png"
  # Adaptive icon (Android 8+): background color + a transparent-background
  # foreground glyph inset in the safe zone, composited by the launcher —
  # NOT the same flat square used for image_path, which bakes the
  # background into the art itself (fine for the legacy/fallback icon, but
  # would double up the background if reused as the adaptive foreground).
  adaptive_icon_background: "#3874EF"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  # Android 13+ themed icon: the system tints this layer's alpha shape with
  # the user's wallpaper-derived color, so it must carry no color of its
  # own -- a white silhouette on transparent, same as any adaptive layer.
  adaptive_icon_monochrome: "assets/icon/icon_monochrome.png"
